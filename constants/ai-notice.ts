// 생성형 AI 사용 고지 (KCAL-17, 서버 `docs/LEGAL_COMPLIANCE.md` §7).
//
// AI기본법 제31조①은 생성형 AI 기반 서비스라는 사실을 **이용 전에** 알리게 하고, 시행령 제23조①이
// 그 방법으로 "이용자 화면에 표시"를 인정한다. 사진 기록의 진입 화면(기록 탭)과 기록 화면에 같은
// 문장을 둔다 — 두 곳의 말이 다르면 어느 쪽이 고지인지 흐려진다.
//
// **AI 가 하는 일만 AI 라고 쓴다.** 칼로리·영양값은 원칙적으로 식약처 DB 실측이고, AI 는 DB 에 없는
// 음식만 추정한다(서버 DATA_MODEL 19장). 모든 수치를 "AI 추정값"이라 부르던 예전 문구는 반대 방향으로
// 틀린 고지였다.
export const AI_USE_NOTICE =
  '음식 인식은 생성형 AI(Google Gemini)가 하고, 칼로리·영양값은 식약처 식품영양성분 DB에서 찾아 계산해요. DB에 없는 음식만 AI가 추정하며, 실제와 다를 수 있어요.';

// 기록이 쌓인 뒤의 화면(홈·지난 기록·진료 탭)에서 합계가 무엇으로 계산됐는지 밝힌다.
export const INTAKE_ESTIMATE_NOTICE =
  '섭취량은 식약처 식품영양성분 DB 값(DB에 없는 음식은 생성형 AI 추정값)으로 계산한 추정치이며 실제와 다를 수 있습니다.';
